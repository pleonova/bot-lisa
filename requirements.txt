fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
httpx>=0.27
rank_bm25>=0.2.2
fastembed>=0.4  # open-source ONNX-based embeddings, see services/retrieval_service/embeddings.py
scikit-learn>=1.4
numpy>=1.26
pytest>=8.0
anthropic>=0.30  # only needed if ANTHROPIC_API_KEY is set (live LLM mode)
slowapi>=0.1.9  # per-IP rate limiting on orchestration-service's public /ask + /assist
